"use strict";
function identityFive(products) {
    console.log(products.length);
    return products;
}
const getMoreSearch = (products) => {
    console.log(products.length);
    const myIndex = 3;
    return products[myIndex];
};
